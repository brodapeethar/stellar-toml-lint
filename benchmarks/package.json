{
  "name": "benchmark",
  "version": "1.0.0",
  "description": "Performance benchmark and stress-testing harness for stellar-toml-lint",
  "type": "module",
  "scripts": {
    "bench": "tsx benchmarks/runner.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "vitest": "^2.1.8"
  }
}
